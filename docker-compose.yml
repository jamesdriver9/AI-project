version: '3.8'

services:
  finance-agent:          
    build: .            
    volumes:             
      - .:/app           
      - ./data:/app/data
    env_file:
      - .env
    stdin_open: true
    tty: true